export type GetAddressesResType = {
  addressId: number;
  addressLabel: string;
  fullAddress: string;
  isDefault: boolean;
}[];

export type AddressType = {
  addressId: number;
  addressLabel: string;
  fullAddress: string;
  isDefault: boolean;
};
